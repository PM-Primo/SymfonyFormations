{% block form_row %}

    <div class="form-group">
        <div class="toto">        
            {{ form_label(form) }}
        </div>
        {{ form_errors(form) }}
        {{ form_widget(form) }}
    </div>

{% endblock %}