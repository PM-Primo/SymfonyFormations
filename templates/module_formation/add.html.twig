{% extends 'base.html.twig' %}

{% block title %}Ajouter un module{% endblock %}

{% block body %}

    {% if edit %}
        <h1>Éditer un module</h1>
    {% else %}
        <h1>Ajouter un module</h1>
    {% endif %}


    {{ form_start(formAddModuleFormation) }}

        <div class="nomPrenom">
            {{ form_row(formAddModuleFormation.nom_module) }}
            {{ form_row(formAddModuleFormation.categorie) }}
        </div>

    {{ form_end(formAddModuleFormation) }}


{% endblock %}